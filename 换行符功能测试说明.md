# text2social 换行符功能测试说明

## 🔧 修复内容

本次修复主要解决了选中文本中换行符丢失的问题：

### 1. 文本选择方式改进
- **之前**: 使用 `selection.toString()` 获取纯文本，丢失HTML结构
- **现在**: 使用 `range.cloneContents()` 获取HTML内容，保留结构

### 2. 文本清理函数优化
- 增强了HTML标签到换行符的转换
- 改进了HTML实体处理
- 优化了空白字符规范化
- 保留了换行符结构

### 3. 模板CSS支持
- 添加了 `white-space: pre-wrap` 支持换行符显示
- Canvas方法改进了换行处理逻辑

## 🧪 测试方法

### 方法1: 使用测试页面
1. 打开 `test-text-cleaning.html` 页面
2. 点击各个测试按钮查看清理效果
3. 注意观察"换行符可视化"部分

### 方法2: 在实际网页中测试
1. 安装并启用text2social扩展
2. 在任意网页选择包含HTML格式的文本，例如：
   ```html
   <p>第一段</p>
   <div>第二段</div>
   <p>第三段</p>
   ```
3. 点击📸按钮生成预览
4. 检查控制台输出的调试信息

### 方法3: 使用调试脚本
1. 在浏览器控制台中加载 `debug-test.js`
2. 运行 `runAllTests()` 执行所有测试
3. 检查输出结果

## 📋 测试用例

### 基本换行测试
- 输入: `<p>第一行</p><div>第二行</div>`
- 期望: `第一行\n第二行`

### 混合标签测试
- 输入: `<strong>粗体</strong><br>换行<em>斜体</em>`
- 期望: `粗体\n换行斜体`

### 列表测试
- 输入: `<ul><li>项目1</li><li>项目2</li></ul>`
- 期望: `项目1\n项目2`

### HTML实体测试
- 输入: `<p>&amp;实体 &lt;测试&gt;</p>`
- 期望: `&实体 <测试>`

## 🔍 调试信息

当选择文本时，控制台会显示：
- `🧹 Original text:` - 原始HTML内容
- `✨ Cleaned text:` - 清理后的文本
- `📏 Line count:` - 文本行数

## ⚠️ 常见问题

### Q: 换行符仍然不显示？
A: 检查以下几点：
1. 确保选中的文本包含HTML换行标签（`<br>`, `</p>`, `</div>`等）
2. 检查浏览器控制台是否有错误信息
3. 确认扩展已正确加载

### Q: 文本格式混乱？
A: 可能是HTML结构复杂，调试脚本可以帮助分析具体问题。

### Q: 如何查看原始HTML？
A: 在测试页面中，点击测试按钮后会显示原始HTML内容。

## 📝 开发者说明

### 关键文件
- `content/content.js:367-417` - 文本清理函数
- `content/content.js:256-270` - HTML内容获取
- `templates/template1.css:91-93` - 换行符CSS支持

### 主要函数
- `cleanSelectedText()` - 文本清理核心函数
- `document.addEventListener('mouseup')` - 文本选择监听

## 🎯 下一步改进

1. 支持更多HTML标签的换行转换
2. 添加用户自定义格式选项
3. 优化表格内容处理
4. 增加更多调试工具