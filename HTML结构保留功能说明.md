# text2social HTML结构保留功能说明

## 🔧 功能概述

本次优化实现了**保留HTML结构但移除所有样式**的文本处理功能：
- **保留**: HTML结构标签（`<div>`, `<p>`, `<ul>`, `<li>`, `<table>` 等）
- **移除**: 样式相关标签（`<strong>`, `<em>`, `<span style="...">` 等）
- **移除**: 所有HTML属性（`style`, `class`, `id` 等）
- **保持**: 原始的HTML布局结构

## 🎯 功能特点

### 1. 智能标签处理
- **样式标签移除**: `<strong>`, `<em>`, `<b>`, `<i>`, `<u>`, `<span>`, `<font>` 等
- **内容保留**: 移除标签但保留其内部文本内容
- **结构保留**: `<div>`, `<p>`, `<ul>`, `<li>`, `<table>`, `<tr>`, `<td>` 等结构标签完整保留

### 2. 属性清理
- 移除所有HTML属性（`style`, `class`, `id`, `data-*` 等）
- 保留标签本身，确保HTML结构完整
- 自动将标签名转换为小写

### 3. HTML实体处理
- 解码常见HTML实体（`&nbsp;`, `&amp;`, `&lt;`, `&gt;` 等）
- 移除不常见的实体
- 确保文本内容正确显示

## 📝 处理示例

### 示例1: 基本样式移除
```html
<!-- 输入 -->
<div><strong>粗体文字</strong><em>斜体文字</em></div>

<!-- 输出 -->
<div>粗体文字斜体文字</div>
```

### 示例2: 属性清理
```html
<!-- 输入 -->
<p style="color:red; font-size:16px;" class="title" id="main">内容</p>

<!-- 输出 -->
<p>内容</p>
```

### 示例3: 复杂结构保留
```html
<!-- 输入 -->
<div style="background:#f0f0f0;">
  <h1><strong>标题</strong></h1>
  <p><em>段落内容</em></p>
  <ul style="list-style-type: square;">
    <li><b>项目1</b></li>
    <li><i>项目2</i></li>
  </ul>
</div>

<!-- 输出 -->
<div>
  <h1>标题</h1>
  <p>段落内容</p>
  <ul>
    <li>项目1</li>
    <li>项目2</li>
  </ul>
</div>
```

## 🧪 测试方法

### 方法1: 测试页面
1. 打开 `test-text-cleaning.html` 页面
2. 点击各个测试按钮查看清理效果
3. 观察"渲染效果预览"部分

### 方法2: 调试脚本
1. 在浏览器控制台中加载 `html-cleaning-debug.js`
2. 运行 `runAllHtmlTests()` 执行测试
3. 运行 `createVisualTest()` 创建可视化测试

### 方法3: 实际使用
1. 在任意网页选择包含HTML格式的文本
2. 点击📸按钮生成预览
3. 检查控制台输出的调试信息

## 🔍 支持的HTML结构

### 结构标签（保留）
- **容器**: `<div>`, `<section>`, `<article>`, `<header>`, `<footer>`, `<nav>`, `<aside>`, `<main>`
- **文本**: `<p>`, `<h1>` - `<h6>`, `<blockquote>`, `<pre>`, `<address>`
- **列表**: `<ul>`, `<ol>`, `<li>`, `<dl>`, `<dt>`, `<dd>`
- **表格**: `<table>`, `<tr>`, `<td>`, `<th>`, `<thead>`, `<tbody>`, `<tfoot>`
- **其他**: `<br>`, `<hr>`, `<figure>`, `<figcaption>`

### 样式标签（移除但保留内容）
- **文本格式**: `<strong>`, `<b>`, `<em>`, `<i>`, `<u>`, `<s>`, `<del>`, `<ins>`
- **语义标记**: `<code>`, `<kbd>`, `<samp>`, `<var>`, `<mark>`, `<abbr>`, `<cite>`, `<dfn>`, `<q>`, `<time>`
- **通用**: `<span>`, `<font>`, `<small>`, `<big>`, `<sub>`, `<sup>`

## 🎨 模板样式支持

模板CSS已更新以支持HTML结构标签的显示：
- **段落**: 合适的间距和对齐
- **列表**: 正确的列表样式和缩进
- **表格**: 美观的边框和间距
- **标题**: 不同层级的字体大小
- **引用**: 特殊的引用样式
- **代码块**: 等宽字体和背景

## 📊 调试信息

处理过程中的控制台输出：
- `🧹 Original HTML:` - 原始HTML内容
- `✨ Cleaned HTML:` - 清理后的HTML内容

## ⚠️ 注意事项

1. **嵌套结构**: 支持多层嵌套的HTML结构
2. **自闭合标签**: `<br>` 和 `<hr>` 标签会被保留但属性被移除
3. **空标签**: 完全空的标签会被保留，维持结构
4. **注释**: HTML注释会被移除
5. **脚本**: `<script>` 和 `<style>` 标签会被完全移除

## 🔧 技术实现

### 核心函数
- `cleanHtmlContent(html)` - HTML清理核心函数
- 正则表达式模式匹配各种HTML标签
- 递归处理嵌套结构

### 关键步骤
1. 移除样式标签（保留内容）
2. 移除HTML属性
3. 处理HTML实体
4. 清理空白字符
5. 规范化标签格式

## 🎯 下一步改进

1. 支持更多HTML5语义标签
2. 添加用户自定义标签过滤规则
3. 优化复杂表格的处理
4. 增加更多调试工具